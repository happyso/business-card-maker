import React from 'react';

// const Header = ({ isLogin }) => {
//   return <>{isLogin && <div className="header">Business Card Maker</div>}</>;
// };

const Header = () => {
  return (
    <>
      <div className="header">Business Card Maker</div>
    </>
  );
};

export default Header;
